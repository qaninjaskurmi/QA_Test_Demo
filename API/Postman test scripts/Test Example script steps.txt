As API test was not in the scope, i do not do the full script here. i just do a test example run script here, 
where we can do a basic test in the postman test script using runner we can call multiple API, 
and Usung Newman we can do a automan runner test in before automantion build by using jenkins.